import { Course } from "@prisma/client"

type purchaseCoursesProps={
    userId:string
    title:string 
    courseId:string
}

export const getPurchaseCourses=async({
    userId,
    title,
    courseId
}:purchaseCoursesProps)=>{
    try {
        
    } catch (error) {
        
    }
}